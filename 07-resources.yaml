apiVersion: v1
kind: Pod
metadata:
 name: resources
spec:
 containers:
 - name: nginx
   image: nginx
   resources:
     request:
       memory: "60Mi"
       cpu: "100m"
     limits:
       memory: "100Mi"
       cpu: "120m"